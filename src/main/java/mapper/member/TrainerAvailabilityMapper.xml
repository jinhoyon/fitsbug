<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.TrainerAvailabilityMapper">

    <resultMap id="availResultMap" type="dto.common.AvailabilityDTO">
        <id     property="id"          column="id"/>
        <result property="trainerId"   column="trainer_id"/>
        <result property="dayOfWeek"   column="day_of_week"/>
        <result property="startTime"   column="start_time"/>
        <result property="endTime"     column="end_time"/>
        <result property="active"      column="is_active"/>
    </resultMap>

    <!-- 가능 시간 저장 -->
    <insert id="insert" parameterType="dto.common.AvailabilityDTO">
        INSERT INTO trainer_availability
            (trainer_id, day_of_week, start_time, end_time, is_active)
        VALUES
            (#{trainerId}, #{dayOfWeek}, #{startTime}, #{endTime}, 1)
    </insert>

    <!-- trainer_id로 활성 스케줄 전체 조회 -->
    <select id="findByTrainerId" parameterType="int" resultMap="availResultMap">
        SELECT id, trainer_id, day_of_week, start_time, end_time, is_active
        FROM trainer_availability
        WHERE trainer_id = #{trainerId}
          AND is_active  = 1
        ORDER BY FIELD(day_of_week,'MON','TUE','WED','THU','FRI','SAT','SUN')
    </select>

    <!-- 특정 요일 조회 -->
    <select id="findByDay" parameterType="map" resultMap="availResultMap">
        SELECT id, trainer_id, day_of_week, start_time, end_time, is_active
        FROM trainer_availability
        WHERE trainer_id = #{trainerId}
          AND day_of_week = #{dayOfWeek}
          AND is_active   = 1
    </select>

    <!-- trainer_id로 전체 삭제 (재등록용) -->
    <delete id="deleteByTrainerId" parameterType="int">
        DELETE FROM trainer_availability WHERE trainer_id = #{trainerId}
    </delete>

</mapper>
