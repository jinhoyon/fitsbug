<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.member.exercise_guide">

    <resultMap id="exerciseResultMap" type="dto.member.ExerciseGuideDTO">
        <id     property="id"           column="id"/>
        <result property="title"        column="title"/>
        <result property="difficulty"   column="difficulty"/>
        <result property="targetMuscle" column="targetMuscle"/>
        <result property="type"         column="type"/>
        <result property="description"  column="description"/>
        <result property="keyPoint"     column="keyPoint"/>
        <result property="image"        column="image"/>
        <result property="video"        column="video"/>
        <result property="regDate"      column="reg_date"/>
    </resultMap>

    <!-- 전체 조회 (ExerciseDAOImpl.getAllExercises) -->
    <select id="getAllExercises" resultMap="exerciseResultMap">
        SELECT id, title, difficulty, targetMuscle, type,
               description, keyPoint, image, video, reg_date
        FROM EXERCISE_GUIDE
        ORDER BY reg_date DESC
    </select>

    <!-- 키워드 검색 (ExerciseDAOImpl.searchExercises) -->
    <select id="searchExercises" parameterType="string" resultMap="exerciseResultMap">
        SELECT id, title, difficulty, targetMuscle, type,
               description, keyPoint, image, video, reg_date
        FROM EXERCISE_GUIDE
        WHERE title        LIKE CONCAT('%', #{keyword}, '%')
           OR targetMuscle LIKE CONCAT('%', #{keyword}, '%')
           OR difficulty   LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY reg_date DESC
    </select>

    <!-- id로 단건 조회 (ExerciseDAOImpl.findById) -->
    <select id="findById" parameterType="int" resultMap="exerciseResultMap">
        SELECT id, title, difficulty, targetMuscle, type,
               description, keyPoint, image, video, reg_date
        FROM EXERCISE_GUIDE
        WHERE id = #{id}
    </select>

    <!-- targetMuscle로 필터 조회 (ExerciseDAOImpl.findByMuscle) -->
    <select id="findByMuscle" parameterType="string" resultMap="exerciseResultMap">
        SELECT id, title, difficulty, targetMuscle, type,
               description, keyPoint, image, video, reg_date
        FROM EXERCISE_GUIDE
        WHERE targetMuscle = #{targetMuscle}
        ORDER BY reg_date DESC
    </select>

</mapper>
