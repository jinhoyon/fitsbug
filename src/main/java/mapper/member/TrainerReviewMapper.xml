<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.TrainerReviewMapper">

    <resultMap id="reviewResultMap" type="dto.member.TrainerReviewDTO">
        <result property="userEmail"  column="user_email"/>
        <result property="trainerId"  column="trainer_id"/>
        <result property="rating"     column="rating"/>
        <result property="content"    column="content"/>
        <result property="imagePath"  column="image_path"/>
        <result property="createdAt"  column="created_at"/>
    </resultMap>

    <!-- ──────────────────────────────────────────────────────
         TrainerReviewDAOImpl.insert(TrainerReviewDTO dto)
         → param: TrainerReviewDTO
         ────────────────────────────────────────────────────── -->
    <insert id="insert" parameterType="TrainerReviewDTO">
        INSERT INTO trainer_review (user_email, trainer_id, rating, content, image_path)
        VALUES (#{userEmail}, #{trainerId}, #{rating}, #{content}, #{imagePath})
    </insert>

</mapper>